# Profile "airgapped": no external network, LLM runs locally through Ollama.
# Recommended for critical infrastructure and isolated IT/OT environments.
# Copy to .env (never commit .env).
DEPLOYMENT_PROFILE=airgapped
LLM_PROVIDER=local

OLLAMA_MODEL=llama3.1:8b
OLLAMA_BASE_URL=http://localhost:11434
